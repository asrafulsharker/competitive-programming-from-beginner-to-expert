#include <bits/stdc++.h>
using namespace std;

int main(){
    while (true)
    {
        int x,y,i;
        cin >>  x >> y;
      for (i = x; i <= y/2; ++i) {
            if (x % i == 0) {
                isPrime = false;
                break;
            }
        }
    }
    
    
}